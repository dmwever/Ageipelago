# `startInventoryPool`

Core Archipelago's `StartInventoryPool`, imported from `Options` and declared as a field on
`Age2Options`. The world adds no behaviour of its own: nothing in `worlds/age2de` reads it, and it
reaches neither slot_data nor XS.

The field name is camelCase because it comes from core AP, not from this world's own naming.
