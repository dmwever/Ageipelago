/* Written by the client's /install command. This committed copy is an empty
 * placeholder so the scripts parse before a seed has been installed; with
 * TECH_COUNT at 0, Techsanity is inert. */

const int TECH_COUNT = 0;

void LoadTechTable() {
    return;
}
